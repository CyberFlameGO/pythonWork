name: Qodana
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

permissions:
  contents: read
  
jobs:
  qodana:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@41d3351872a2104cca2e1c0c05eae533d14f3967
        with:
          egress-policy: audit # TODO: change to 'egress-policy: block' after couple of runs

      - uses: actions/checkout@85e6279cec87321a52edac9c87bce653a07cf6c2
      - name: 'Qodana Scan'
        uses: JetBrains/qodana-action@daefe061acb0f8aed7a81658029a29d8ed5cff00
        with:
          linter: jetbrains/qodana-python
